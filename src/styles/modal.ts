import { makeStyles } from '@material-ui/core/styles';

export const useModalStyles = makeStyles((theme) => ({
    root: {
        backgroundColor: '#fff',
        margin: 15,
        padding: 15,
        borderRadius: 5
    }
}));
